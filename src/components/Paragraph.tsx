import styled from 'styled-components/native'
import { Body, Spacing } from '../primitives'

const Paragraph = styled(Body)`
  padding-vertical: ${Spacing.small};
  padding-horizontal: ${Spacing.semiLarge};
`

export default Paragraph
